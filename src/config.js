// Development-only settings. Replace these with local configuration loading before release.
export const modelConfig = {
  baseUrl: "https://your-api-host.example/v1",
  apiKey: "",
  model: "your-model-name",
  systemPrompt: "You are a helpful AI assistant. Reply in Chinese.",
  temperature: 0.7,
  maxTokens: 2000,
};
